import { StyleSheet } from "react-native";

export const styles=StyleSheet.create({
    container:{
        flex:2,
        backgroundColor:'indianred'
    },
    text:{
        fontSize:30,
        color:'white',
        textAlign:'center'

    }
})